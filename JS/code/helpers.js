// INCLUDES 

// INCLUDES -> verifica se tem oque voce procura dentro de um array, string e etc


const tech = 'Node.js'

const includes = tech.includes('Node')
//----->console.log({includes})

// Se oque voce procura estiver dentro, ele vai retornar true

/*---------------------------------------------------------------------*/

// startsWith 

// startsWith -> Verifica se uma string comeca com uma sequencia de caracteres

const startsWith = tech.startsWith('o')
//----->console.log({startsWith})

// Se oque voce procura estiver dentro, ele vai retornar true

/*---------------------------------------------------------------------*/

// endsWith

// endsWith -> Verifica se uma string yrtmina com uma sequencia de caracteres

const endsWith = tech.endsWith('e')
//----->console.log({endsWith})

// Se oque voce procura estiver dentro, ele vai retornar true